import * as React from "react";

const ProjectsSvg = ({
  label,
  className = "",
  variant = "contained",
  color = "primary",
  ...rest
}) => {
  return <div></div>;
};

export default ProjectsSvg;
