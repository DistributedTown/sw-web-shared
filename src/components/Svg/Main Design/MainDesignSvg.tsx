import * as React from "react";

const SwMainDesignSvg = ({
  label,
  className = "",
  variant = "contained",
  color = "primary",
  ...rest
}) => {
  return <div></div>;
};

export default SwMainDesignSvg;
