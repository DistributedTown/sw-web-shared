import { SWIDParams, SWIDOutput } from './Badge.model';
export declare const SkillWalletIDBadgeGenerator: ({ canvas, avatar, tokenId, title, timestamp, config, }: SWIDParams) => Promise<SWIDOutput>;
